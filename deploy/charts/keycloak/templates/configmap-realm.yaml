{{- if .Values.realm.import }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "keycloak.fullname" . }}-realm
  labels:
    {{- include "keycloak.labels" . | nindent 4 }}
data:
  hfwas-devops-realm.json: |
{{ .Files.Get "files/realm.json" | indent 4 }}
{{- end }}
