{{- if .Values.schema.init.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "backend.fullname" . }}-schema
  labels:
    {{- include "backend.labels" . | nindent 4 }}
    app.kubernetes.io/component: schema
data:
{{ (.Files.Glob "files/db/*").AsConfig | indent 2 }}
{{- end }}
