# delivery-platform 子项目忽略规则
#
# 仓库根 .gitignore 的 /dist/ 只锚定根目录，覆盖不到这里；
# data/ 与 *.db 已由根规则覆盖（此处不重复），其余为子项目特有产物。

### 前端：依赖与构建产物 ###
node_modules/
dist/
.vite/

### 包管理器日志与缓存 ###
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm/
.yarn/
.pnpm-store/

### Go 构建与测试产物 ###
bin/
*.test
*.out