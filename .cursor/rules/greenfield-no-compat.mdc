---
description: 从 0 到 1 项目：改字段/模型直接改，不做旧数据兼容
alwaysApply: true
---

# 绿野项目：不做存量兼容

本仓库是 **从 0 到 1** 的产品，本地/开发库可随时重建，**不要**为已有数据库内容做向后兼容。

## 必须遵守

- 改表结构、列名、JSON 形状、API 字段时：**直接改** schema / entity / VO / 前端类型，保持单一真相。
- **不要**写：双写旧列、legacy 字段合成、`if (oldFormat)` 分支、仅用于迁移旧数据的 runtime migration（除非用户明确要求保留某次迁移）。
- **不要**为「旧客户端只传某字段」保留长期兼容路径；以当前文档与类型为准。
- 需要清库或重跑 `pm-schema.sql` / 启动脚本时，优先重建，而不是打补丁兼容脏数据。

## 允许的例外

- 用户**明确**要求兼容某版本数据或线上迁移时，再写一次性迁移脚本，并在文档中标明可删除期限。
- 同一次 PR 内的「改完即删」的临时桥接可以有，合并前应去掉。

## 与现有代码

若发现 `transitions` ↔ `transitionRules` 双写、空规则合成等仅为兼容旧行的逻辑，后续触及该模块时应**收敛为单一权威结构**，删除冗余列/分支。
